public abstract class Animal implements Asset{
    
    public abstract String speak();
         //redundant, unused method. no animal objects allowed to create anymore
        //every animal must have a speak method but left to child classes

}
